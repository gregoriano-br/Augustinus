{
  "name": "augustinus-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build",
    "start:frontend": "bun run --filter=augustinus-frontend dev",
    "start:cli": "bun run --filter=@augustinus/cli start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.0.0"
  }
}